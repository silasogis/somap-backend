POSTGRES_DB=somap
POSTGRES_USER=somap
POSTGRES_PASSWORD=somap_secret

DATABASE_URL=postgresql+asyncpg://somap:somap_secret@db:5432/somap

JWT_SECRET=troque_por_valor_aleatorio_longo
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=480

GEOSERVER_URL=http://geoserver:8080/geoserver
GEOSERVER_ADMIN_USER=admin
GEOSERVER_ADMIN_PASSWORD=geoserver

# URLs públicas do Cloudflare Tunnel — preencher após criar o tunnel
API_PUBLIC_URL=https://api.somap.<seu-dominio>.com
GEO_PUBLIC_URL=https://geo.somap.<seu-dominio>.com

# Origins permitidas no CORS: GitHub Pages + localhost para dev local
CORS_ORIGINS=https://<usuario>.github.io,http://localhost:5173

# Caminho local para arquivos raster (usado no GeoServer)
RASTER_PATH=/home/umbrel/dados/raster

